import DemoApp from '@/demo/DemoApp'

// The demo ships its own navigation (tabs, account chip, and a brand link
// back to the main site), so this page renders it directly.
export default function Demo() {
  return <DemoApp />
}
